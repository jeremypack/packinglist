<h2 class="underline">Packing Templates</h2>
<table>
	<% for bag in @bag_templates do %>
	<tr>
		<td width="250px">
			<%= bag.name %>
		</td>
		<td><%= link_to 'Use Template', use_template_bag_path(bag), :method => :post, :class => "link_button" %></td>
	</tr>
	<% end %>
	
</table>
