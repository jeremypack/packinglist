<% form_for :item, :url => items_path, :html => {:multipart => true} do |f| %>
	
	<!-- <p> -->
		<%= f.label :name %>:
		<%= f.text_field :name %>
		
		<%= hidden_field_tag :category_id, 8 %>

		<%= submit_tag 'Create' %>

<% end  %>